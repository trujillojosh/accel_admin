<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapsed" data-target="#navbar-collapse" >
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" >
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to 'RC', root_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if login_signed_in? %>
          <% if current_login.admin == true %>
            <li><%= link_to 'Teams', '/teams' %></li>
            <li><%= link_to 'Users', '/users' %></li>
            <li><%= link_to 'Attendance', '/bulk_attendances/new' %></li>
            <li><%= link_to 'Weekly Meeting', '/all_hands/new' %></li>
            <li><%= link_to 'Team Form' , '/teammakings/'</li>
          <% end %>
          <li><%= link_to 'Correction', '/corrections/new' %></li>
        <% end %>
        <% if login_signed_in? %>
          <li><%= link_to('Logout', destroy_login_session_path, :method => :delete) %></li>
        <% else %>
          <li><%= link_to('Login with 42', login_marvin_omniauth_authorize_path) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>