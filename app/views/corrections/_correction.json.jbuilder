json.extract! correction, :id, :team_id, :corrector, :date, :pitch, :demo, :b_kpi_a, :t_kpi_a, :help_a, :blockers_a, :other, :created_at, :updated_at
json.url correction_url(correction, format: :json)
